{
  "name": "promptctl-root",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "test": "turbo run test"
  },
  "engines": {
    "node": ">=18"
  }
}
